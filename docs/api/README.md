# docs/api/

API documentation for PMOS interfaces.

## Conventions

- Document each endpoint or interface: purpose, inputs, outputs, and errors.
- Keep docs in sync with the implementation in the same change.
- Prefer a generated spec (e.g., OpenAPI) as the source of truth once the API
  lands; this directory hosts the spec and supporting guides.
